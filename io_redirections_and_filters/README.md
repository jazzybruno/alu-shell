# 0x02. Shell, I/O Redirections and filters

Two-line Bash scripts covering stdout/stderr redirection, piping between
filters (head, tail, grep, cut, tr, sort, uniq, rev, find), and parsing
/etc/passwd-style and TSV log data — no sed/awk.
